# フロントのオリジン（複数はカンマ区切り）。未設定時は全許可。開発時は 4200/4242 を自動追加
CORS_ORIGIN=
# 認証トークン署名用（必須）。例: openssl rand -hex 32
JWT_SECRET=
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_ROOT_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=
DATABASE_URL=
EXPOSE_BACKEND_PORT=
EXPOSE_DATABASE_PORT=
